%h1 Alarms

= form_for @alarm, remote: true do |f|
  .form-group
    = f.text_area :description, rows: 4, class: 'form-control', placeholder: 'Enter alarm text...'
  .form-group
    = f.submit 'Send', class: 'btn btn-info'

.alarms-container
  = render @alarms
